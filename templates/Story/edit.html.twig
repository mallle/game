{% extends 'base.html.twig' %}


{% block headline %}
	{% if story.id %}
		Edit story:
		{{ story.name }}
	{% else %}
		Create new story
	{% endif %}
{% endblock %}


{% block body %}
	{{ form(form) }}{% endblock %}
